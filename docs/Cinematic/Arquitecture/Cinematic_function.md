Create a plan to create a new service call the cinematic service, the service generate the full video based on query question, for this you should create a new page Cinematic taht will be called when user select Lab/studio (so update the home page thre slector) this new page is based on the current studio page and also the new cinemativ workflow  you already made and the new supabase function to call Veo video, and also takes insperation from the image attached. the new cinematic service instead of using the current flow that look for images and animate a video, make a this new flow that just invoque Veo Video (with out new tested working supabase function), and return full 8 sec video (the scene), the scenes (3 to 5) ate shown in a carrusel just below the video  (generate landscape on desktop and square on mobile), the video include the tipical controllers that you already have , but maybe can copy the ones in the image attached.  Below the video add the scenes that is the image of each video generated (since veo genrate 8 sec videosour explanation are made by from 3 to 5 videos from veo so full movie is 24 to 40 sec) but let the director agent decide on how many scenes he need updating to add this restrictions (max 8 sec veo videos) also let's make call to video in  parallel (maybe wait 1 sec betwenn call to do not get API rate limit). below the  scenes are the part you already have video title  like share save like feedback as shown in the attached image, the full video text  and the sources or quick links, and below is a new section (inspired on the image) Sugested Visual answer or continue exploring , Related topics and generate 4 headlines for related topics / continue exploring and based on each topic generate one image. Finally revie the workflow and be sure one agent or create a new one refrase the query to look invoque tavily search engine, and the serach results are loading to the llm call so the answer and video are rooted trusted sources , also add the source in the tab based on the search services. 