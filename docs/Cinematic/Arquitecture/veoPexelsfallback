let's try to make it work so le't look a the logic that call the veo service, follow the oficial documentation and implement a direct simple path, first call first scene in order, each scene including the first and following should have a simple status of the task: created., procesing, completed (give up to 30 sec to complete), or failed if call fail  just rety one time, if fail agian or timeout just fall back to pexels video instead (for taht look and reuse the implementation we used for Studio with videos no stock photo). To avoid having multiples process running in parallel uses a simple rule like waiting 8 second  (video duration) between each call and and implement a simple queue to keep max 2 videos in procesing status, when fideo finish add in the previus section update as completed and call next video taking atention to not have more than 2 videos procesing at a time. check each 8 second or when necesary to avoid over pulling Veo service, Finally refactor the director to make a shorter version of video between 2 and 4 scenes max (in case of veo8 sec in case of pexels take whaever it gives you but cap at 10 sec if longer)

