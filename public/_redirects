# Proxy Supabase Edge Functions directly (no Netlify proxy needed)
# EXCEPT social-share which needs Netlify to force correct Content-Type
/functions/v1/social-share /.netlify/functions/social-share 200

# Fast Search share: Supabase proxy attempt — didn't work reliably for previews,
# so Fast Search uses the Netlify social-share function instead (see
# buildSocialShareUrl.ts). Kept commented for reference.
# /api/social-share https://gpfccicfqynahflehpqo.supabase.co/functions/v1/social-share 200

# Dynamic sitemap of public Curiosity nodes (SEO/GEO discovery)
/sitemap.xml /.netlify/functions/sitemap 200

# SPA fallback - must be last
/* /index.html 200